import type { GameMeta } from "./types";

/**
 * Games built in the `steve` worktree.
 *
 * Owned by Steve — Alex does not edit this file. Same rule as the alex
 * registry: metadata only, no game imports.
 */
export const STEVE_GAMES: GameMeta[] = [];
